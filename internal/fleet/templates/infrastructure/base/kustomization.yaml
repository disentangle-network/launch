apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
# Resources are added per-cluster via overlays:
#   - cert-manager (if TLS needed)
#   - nebula-pq DaemonSet (if mesh enabled)
